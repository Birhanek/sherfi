import { NextFunction, Request, Response } from "express";



/**
 * To send registration confirmation to the email of the user
 * @param req :Request type express
 * @param res :Response type express
 * @param next :Next Function type express
 */
const authorRegister = async(req:Request,res:Response,next:NextFunction) =>{
    try {
        
    } catch (error) {
        
    }
}

const authorVerify = async(req:Request,res:Response,next:NextFunction) => {

}